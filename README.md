# Billr (Work in Progress)

## Getting started

```bash
cd frontend && npm i
```

Running the app

Install the `expo go` app on your phone, then run:

```bash
npx expo start
```

Scan the QR code generated
